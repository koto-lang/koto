@main = ||
  n = koto.args.first()?.to_number() or 4

  a = []
  for i in 0..n
    a = (10..200).enumerate().to_tuple()

@test it_works = ||
  x = (1..=3).enumerate().to_tuple()
  assert_eq x, ((0, 1), (1, 2), (2, 3))
