# This file is imported by main.koto

# std modules should be available to import
import test.assert_eq
import math

assert_eq math.pi math.pi

# main functions should be run when loading a module
export main = ||
  export qux = "O_o"
