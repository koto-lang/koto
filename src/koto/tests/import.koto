import test.assert_eq

# importing a module brings the module's global map into scope by assignment
# test_module is defined in the test_module directory, with main.koto as its entry point
import test_module
assert_eq (type test_module) "map"
assert_eq test_module.foo 42

# import statements can appear at any point in a script, and can refer directly to items
x = import test_module.bar
assert_eq bar -1
assert_eq x bar

import test_module.baz.qux
assert_eq qux "O_o"

# import statements can bring in multiple items via list/from syntax
import [foo bar] from test_module
assert_eq foo 42
assert_eq bar -1
