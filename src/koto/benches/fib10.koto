import env

fib = |n|
  match n
    _ if n <= 0 then 0
    1 then 1
    _ then (fib n - 1) + (fib n - 2)

export main = ||
  n = match env.args.get 0
    () then 8
    arg then arg.to_number()

  fib n
