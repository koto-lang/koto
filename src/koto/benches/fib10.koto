import env list

fib = |n|
  if n <= 0
    0
  else if n == 1
    1
  else
    (fib n - 1) + (fib n - 2)

export main = ||
  n = list.get env.args 0
  if n == ()
    n = 8
  else
    n = number n

  fib n
