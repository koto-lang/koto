import env list string

fib = |n|
  match n
    _ if n <= 0 then 0
    1 then 1
    _ then (fib n - 1) + (fib n - 2)

export main = ||
  n = match list.get env.args 0
    () then 8
    arg then string.to_number arg

  fib n
