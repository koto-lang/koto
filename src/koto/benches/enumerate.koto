import env list

enumerate = |xs| [[i x] for i, x in 0..size(xs), xs]

export main = ||
  n = match list.get env.args 0
    () then 10
    arg then number arg

  a = []
  for i in 0..n
    a = [[c d] for c, d in (enumerate 10..20)]
