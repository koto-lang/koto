import env

enumerate = |xs| [[i x] for i, x in 0..xs.length(), xs]

export main = ||
  n = match env.args.get 0
    () then 10
    arg then arg.to_number()

  a = []
  for i in 0..n
    a = [[c d] for c, d in (enumerate [10..20])]
